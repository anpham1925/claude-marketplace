#!/usr/bin/env bash
# Evidence-Based Rule — injected into every user prompt to enforce verified claims.
# Companion to skill-first.sh. Backs the rules/evidence-based.md content.

cat <<'HOOK_OUTPUT'
{"hookSpecificOutput":{"hookEventName":"UserPromptSubmit","additionalContext":"BLOCKING REQUIREMENT — EVIDENCE-BASED RULE\n\nEvery non-obvious factual claim about code, libraries, APIs, runtime behaviour, prior conversations, or external systems MUST be backed by verifiable evidence BEFORE you commit to it — in user-facing text, code edits, tool calls, or external sends. This applies to EVERY turn in EVERY session. No exception for internal exploration vs external comms.\n\nFOR EACH LOAD-BEARING CLAIM, tag it explicitly:\n• VERIFIED — cite path:line, doc URL, or output of a command you actually ran THIS session\n• INFERRED — state the inference + what could make it wrong\n• ASSUMED — STOP and verify (Read/Bash/WebFetch/grep installed package) BEFORE acting, or flag explicitly to the user\n\nFORBIDDEN:\n• Pattern-matching to similar systems as proof (e.g. 'X library does Y because Temporal/Kafka/Postgres works that way')\n• Restating an earlier assumption as established fact in later turns\n• Hallucinated APIs, file paths, function names, command flags, version numbers\n• Confident language ('definitely', 'always', 'guaranteed', 'will') without verified evidence\n• Citation laundering ('best practice says...', 'the docs recommend...') with no named source\n• Phantom file references — citing src/foo.ts:123 when you haven't actually read that file this session\n\nWHEN UNCERTAIN: say 'I don't know — let me check X' and verify before answering. A clean 'I don't know' beats a confident wrong answer.\n\nFull rule: engineering-toolkit/rules/evidence-based.md\n\nFAILURE TO BACK A LOAD-BEARING CLAIM WITH EVIDENCE IS A VIOLATION."}}
HOOK_OUTPUT
